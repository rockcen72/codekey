node_modules/
dist/
.env
*.local
~/.devtap/
.devcontainer/
.vscode/
package-lock.json

# Build artifacts
*.tsbuildinfo
*.vsix
*.js.map
packages/server/relay.cjs

# Bundled bridge output (esbuild)
packages/shared/src/bridge/claude-transcripts.js
packages/shared/src/bridge/claude-transcripts.js.map

# Private project config
project.private.config.json

# Superpowers planning docs (tracked docs unaffected)
docs/superpowers/

# Tooling caches
.codegraph/
.superpowers/
.codex/

# Images / screenshots
*.png
apps/miniprogram/assets/

# Logs
*.log

# Runtime state store
data/

# Temp / PoC
tmp/
.tmp/

# Pre-deploy server backups (timestamped snapshots, see docs/server-deployment.md)
backups/

# Admin panel (WIP)
packages/admin/

# Subscription / commercialization plans (not for VCS)
docs/plans/subscription-plan.md

# OS
.DS_Store
Thumbs.db

# Documentation (not for VCS)
# All docs/ and *.md files are working notes / release records; they
# stay out of git from now on. Already-tracked docs (CLAUDE.md, audit,
# release reference, B1 impact matrix, deployment guide, etc.) are
# left in history — per the same "tracked docs unaffected" policy that
# the original docs/superpowers/ exclude used.
docs/
*.md
