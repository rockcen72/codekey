/**
 * CodeKey VS Code extension constants.
 *
 * FEISHU_APP_ID_CONST — the built-in Feishu enterprise app ID.
 * The VS Code setting `codekey.feishuAppId` overrides this when non-empty.
 * An empty setting value falls back to this constant.
 */
export const FEISHU_APP_ID_CONST = 'cli_aabba46d2a3c9bcb';
