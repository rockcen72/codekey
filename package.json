{
  "name": "codekey",
  "version": "0.1.0",
  "private": true,
  "description": "CodeKey - Remote control for AI coding agents — approve, deny, reply from your phone",
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "test": "npm run test --workspaces",
    "lint": "tsc --noEmit && biome check ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.7.0",
    "@biomejs/biome": "^1.9.0",
    "@types/node": "^22.0.0"
  }
}
