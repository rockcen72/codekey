.container { display: flex; flex-direction: column; align-items: center; padding: 200rpx 40rpx 0; }
.loading-icon { width: 80rpx; height: 80rpx; border: 6rpx solid var(--ck-primary); border-top-color: transparent; border-radius: 50%; animation: spin 1s linear infinite; margin-bottom: 32rpx; }
@keyframes spin { to { transform: rotate(360deg); } }
.success-icon { width: 120rpx; height: 120rpx; line-height: 120rpx; text-align: center; background: var(--ck-success); color: #fff; border-radius: 50%; font-size: 32rpx; font-weight: 700; margin-bottom: 32rpx; }
.fail-icon { width: 120rpx; height: 120rpx; line-height: 120rpx; text-align: center; background: var(--ck-danger); color: #fff; border-radius: 50%; font-size: 32rpx; font-weight: 700; margin-bottom: 32rpx; }
.status-text { font-size: 36rpx; font-weight: 600; margin-bottom: 16rpx; }
.code-display { font-size: 28rpx; color: var(--ck-subtle); font-family: monospace; }
.hint-text { font-size: 28rpx; color: var(--ck-subtle); }
.error-msg { font-size: 28rpx; color: var(--ck-danger); text-align: center; margin: 16rpx 0 48rpx; }
.retry-btn { width: 400rpx; height: 80rpx; line-height: 80rpx; background: var(--ck-primary); color: #fff; border-radius: 40rpx; font-size: 30rpx; }
